#include<stdio.h>
int main()
{
    int n, arr[n];
    
    return 0;

}

/*
Array = {1, 2, 3, 4, 5}
       1)      C doesn't allow declaring an array whose number of elements is not known at the complile time 
 Therefore, the following array declarations are illegal in C 
    int arr[]; //Wrong 
    int n, arr[n]; //Wrong 

*/